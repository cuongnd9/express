html
	head
		title Simple Express • #{screenName}
	body
		h2(style='color:tomato') Users
		a(href='/users/create') New User
		form(action='/users/search', method='GET')
			input(type='text', name='q', value=q)
			button Search
		ul 
			each user in users
				li
					a(href='https://github.com/' + user.name, target='_blank') #{user.name}
		a(href='/') Back Home